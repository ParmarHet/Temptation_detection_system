[project]
name = "temptation-detection-system"
version = "0.1.0"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "backend.app.main:app"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"